/**
 * 世界指南 — 玩家可以随时查阅的公开信息
 *
 * 分两层：
 * 1. 开场说书（游戏开始时自动显示）
 * 2. /world 命令（随时查看详细世界信息）
 *
 * 这里的信息都是"角色应该知道的常识"，不含剧情秘密。
 */

import chalk from 'chalk'

/** 开场说书 — 游戏开始时显示一次 */
export function renderPrologue(): string {
  return `
${chalk.yellow('━'.repeat(50))}
${chalk.yellow.bold('  说书人的话')}
${chalk.yellow('━'.repeat(50))}

${chalk.dim('  "让我先告诉你，这个世界是什么样的。"')}

  这里是${chalk.bold('破晓镇')}——灰脊山脉东麓山谷中的一个小镇。
  大约五百人在这里生活，靠${chalk.cyan('矿石')}和${chalk.cyan('贸易')}过日子。
  每天清晨，第一缕阳光会照亮镇中心的一座${chalk.yellow('古老石碑')}，
  上面刻着无人能读的符文。小镇因此得名。

  ${chalk.bold('最近出了事。')}

  三周前，矿道深处传来${chalk.red('异响')}。两名矿工${chalk.red('失踪')}。
  搜救队进去后也${chalk.red('没有回来')}。
  矿工们开始拒绝下矿。镇上的气氛变得紧张。
  ${chalk.cyan('冒险者公会')}发布了紧急委托：调查矿道，寻找失踪者。

  而你——一个失去记忆的旅人——恰好在这时来到了这里。

${chalk.dim('  "记住，在这个镇上，每个人都有自己的故事。')}
${chalk.dim('   有些人会帮你，有些人在隐瞒什么。')}
${chalk.dim('   保持警觉，多和人聊天，不要轻信任何人。"')}

${chalk.yellow('━'.repeat(50))}
`
}

/** /world 命令 — 详细世界信息 */
export function renderWorldGuide(): string {
  return `
${chalk.cyan('╔══════════════════════════════════════════╗')}
${chalk.cyan('║')}${chalk.bold.white('          破 晓 镇 · 世 界 指 南          ')}${chalk.cyan('║')}
${chalk.cyan('╚══════════════════════════════════════════╝')}

${chalk.bold('📍 地理')}

  破晓镇坐落在山谷中，四周环绕着不同的地形:

  ${chalk.green('南/东')} — ${chalk.bold('暮色森林')}
    茂密的老林，越深越暗。有狼群和哥布林出没。
    林中有废弃的伐木场和猎人小屋。
    ${chalk.dim('危险等级: ★★☆☆☆')}

  ${chalk.red('北')} — ${chalk.bold('灰脊矿道')}
    镇子的经济命脉。分上、中、下三层。
    上层还在运营，中层已废弃，下层无人探索。
    ${chalk.dim('危险等级: ★★★★☆')}
    ${chalk.red('⚠ 最近发生失踪事件，矿工拒绝进入中层以下')}

  ${chalk.yellow('西')} — ${chalk.bold('碎石荒原')}
    干燥崎岖的灰色荒原，遍布碎石和浓雾。
    据说是远古战场，地下有被遗忘的坟冢。
    ${chalk.dim('危险等级: ★★★☆☆')}

${chalk.bold('🏛 镇上的重要地点')}

  ${chalk.cyan('碎盾亭酒馆')} — 冒险者的聚集地，老板叫格雷格
  ${chalk.cyan('冒险者公会')} — 接任务、获取情报的地方
  ${chalk.cyan('铁砧铺')}     — 矮人铁匠经营，买卖武器护甲
  ${chalk.cyan('草药堂')}     — 买药水和草药
  ${chalk.cyan('镇长府')}     — 镇长维克多的办公地
  ${chalk.cyan('晨光石碑')}   — 镇中心的古代石碑，符文至今无人能解

${chalk.bold('⚔️ 你该知道的')}

  · 你是一个${chalk.bold('失去记忆')}的旅人，只记得自己的名字和职业
  · 有人让你来破晓镇，但你不记得是谁
  · ${chalk.cyan('冒险者公会')}在招人——这是赚钱和获取信息的好地方
  · 和${chalk.bold('NPC 交谈')}可以获得线索，信任度越高知道的越多
  · 遇到危险可以${chalk.bold('战斗')}或${chalk.bold('逃跑')}，量力而行
  · ${chalk.bold('/npc')} 查看你认识的人，${chalk.bold('/status')} 查看角色状态

${chalk.bold('💡 提示')}

  ${chalk.dim('· 格雷格的酒馆是最好的起点——他消息灵通')}
  ${chalk.dim('· 注意观察 NPC 的异常行为，可能是线索')}
  ${chalk.dim('· 不是所有问题都需要武力解决')}
  ${chalk.dim('· 有些地方需要先完成任务才能安全进入')}

${chalk.cyan('──────────────────────────────────────────')}
${chalk.dim('  输入 /npc 查看人物档案 | /map 查看地图 | /help 查看所有命令')}
`
}
